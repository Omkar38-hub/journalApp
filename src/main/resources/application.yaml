spring:
  profiles:
    active: prod

logging:
  level:
    net:
      project:
        journalApp: TRACE
    org.springframework.data.redis: DEBUG

#debug: true
springdoc:
  swagger-ui:
    path: /docs
